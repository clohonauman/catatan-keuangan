# Catatan Keuangan - PRIVATE DIRECTORY
# Folder ini tidak boleh diakses langsung melalui HTTP/URL.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
